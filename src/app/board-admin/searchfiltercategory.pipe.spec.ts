import { SearchfilterPipeCatgeory } from './searchfiltercategory.pipe';

describe('SearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfilterPipeCatgeory();
    expect(pipe).toBeTruthy();
  });
});